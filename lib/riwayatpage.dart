import 'package:flutter/material.dart';

class RiwayatDeteksiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Riwayat Deteksi'),
      ),
      body: Center(
        child: Text('History'),
      ),
    );
  }
}
