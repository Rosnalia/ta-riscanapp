import 'package:flutter/material.dart';

class TidakLayakPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('TidakLayak Rice'),
      ),
      body: Center(
        child: Text('TidakLayak Rice Detail Page'),
      ),
    );
  }
}